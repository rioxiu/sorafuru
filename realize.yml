settings:
  files:
    - ./**/*.go
  ignored_directories:
    - .git/
    - .idea/
    - vendor/
  no_rebuild_ext:
    - .tpl
web:
  script: main.go
  address: localhost:8080
  environment:
    APP_ENV: development
